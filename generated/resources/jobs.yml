resources:
  jobs:
    alteryx_processor_job:
      name: alteryx_processor_job

      tasks:
        - task_key: migrate

          spark_python_task:
            python_file: ../src/workflow.py

          environment_key: default

      environments:
        - environment_key: default
          spec:
            client: "1"